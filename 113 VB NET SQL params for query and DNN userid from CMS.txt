

        Dim params As Dictionary(Of String, String) = New Dictionary(Of String, String)
        params.Add("modulename", "ViewContract")
        params.Add("severity", severity)
        params.Add("activity", activity)
        params.Add("subid", subid)
        params.Add("contractType", contractType)

        Dim userInfo As New DotNetNuke.Entities.Users.UserInfo
        userInfo = UserController.GetCurrentUserInfo()
        params.Add("userid", userInfo.UserID)
        params.Add("username", userInfo.Username)

        CALog.InsertLog(params)